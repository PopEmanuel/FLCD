func main ()
{
	let int a := readFromKb();
	let int b := readFromKb(), c := readFromKb();
	
	let int max := 0;
	
	if ( a > max ){
		max := a;
	}
	if ( b > max ){
		max := b;
	}
	if ( c > max ){
		max := c;
	}

	writeToScr( max );

}